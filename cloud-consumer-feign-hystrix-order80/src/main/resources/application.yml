server:
  port: 80
eureka:
  client:
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:7001/eureka/

#fegin调用开启hystrix
feign:
  hystrix:
    enabled: true
#feign:
  #client:
      #config:
        #default:
        #connect-timeout: 3000
         #read-timeout: 3000